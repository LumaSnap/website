# LumaSnap Website

Static product website for LumaSnap.

## Local Preview

Open `index.html` directly in a browser.

## Deployment

The site is designed for Cloudflare Pages. The current build output is the repository root and does not require a build command.
